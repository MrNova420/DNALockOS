# DNA-Key Authentication System - Mobile/Termux Dependencies
# Version: 1.0
# Use this file for mobile devices (Termux) where native compilation is limited
# Usage: pip install -r requirements-mobile.txt

# Core Cryptographic Libraries
PyNaCl==1.5.0              # libsodium bindings (Ed25519, X25519, AES-256-GCM)
cryptography>=43.0.0,<47.0.0  # Additional crypto primitives
argon2-cffi==23.1.0        # Argon2id password hashing

# Data Serialization
cbor2==5.6.0               # CBOR serialization for DNA keys

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0            # Data validation
pydantic-settings==2.1.0   # Settings management

# Cache (works without PostgreSQL)
redis==5.0.1               # Redis for caching (optional - degrades gracefully)

# Utilities
python-dotenv==1.0.0       # Environment variable management
python-dateutil==2.8.2     # Date utilities
pyyaml==6.0.1              # YAML configuration

# Security
python-jose[cryptography]==3.3.0  # JWT handling
passlib[bcrypt]==1.7.4     # Password hashing utilities
python-multipart==0.0.6    # Form data parsing

# CLI Tools
click>=8.1.7
rich>=13.7.0

# Note: The following are excluded for mobile due to native compilation issues:
# - psycopg2-binary (requires pg_config - use in-memory storage instead)
# - asyncpg (requires PostgreSQL dev headers)
# - mkdocs, mkdocs-material (documentation - not needed at runtime)
# - black, flake8, mypy, isort (dev tools - not needed at runtime)
# - prometheus-client (monitoring - optional)
# - structlog (optional - uses built-in logging fallback)
