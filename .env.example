# DNA-Key Authentication System - Environment Configuration
# Copy this file to .env and customize for your deployment

# ============= Backend API Configuration =============
# API server host (use 0.0.0.0 to listen on all interfaces)
DNAKEY_API_HOST=0.0.0.0

# API server port
DNAKEY_API_PORT=8000

# Enable auto-reload in development (set to false in production)
DNAKEY_API_RELOAD=true

# ============= Frontend Configuration =============
# Frontend server port
DNAKEY_FRONTEND_PORT=3000

# API URL for frontend to connect to backend
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============= Security Configuration =============
# JWT secret key (generate with: openssl rand -hex 32)
# IMPORTANT: Change this in production!
DNAKEY_JWT_SECRET=change-this-to-a-secure-random-value-in-production

# Session timeout in minutes
DNAKEY_SESSION_TIMEOUT=30

# Challenge expiry time in minutes
DNAKEY_CHALLENGE_EXPIRY=5

# ============= Database Configuration (Future) =============
# PostgreSQL connection (when database support is added)
# DNAKEY_DB_HOST=localhost
# DNAKEY_DB_PORT=5432
# DNAKEY_DB_NAME=dnakey_auth
# DNAKEY_DB_USER=dnakey
# DNAKEY_DB_PASSWORD=secure_password_here

# Redis connection (when Redis support is added)
# DNAKEY_REDIS_HOST=localhost
# DNAKEY_REDIS_PORT=6379
# DNAKEY_REDIS_PASSWORD=

# ============= Logging Configuration =============
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
DNAKEY_LOG_LEVEL=INFO

# Log directory
DNAKEY_LOG_DIR=logs

# ============= CORS Configuration =============
# Allowed origins for CORS (comma-separated)
DNAKEY_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# ============= Rate Limiting (Future) =============
# Requests per minute per IP
# DNAKEY_RATE_LIMIT=60

# ============= Admin Configuration =============
# Admin API key (generate with: openssl rand -hex 32)
# IMPORTANT: Change this in production!
DNAKEY_ADMIN_API_KEY=change-this-to-a-secure-admin-key-in-production
